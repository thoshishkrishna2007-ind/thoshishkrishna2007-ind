name: update-stats

on:
  schedule:
    # Runs daily to refresh analytics
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-snake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Generate Snake (refresh)
        uses: Platane/snk@v3
        with:
          github_user_name: thoshishkrishna2007-ind
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake.gif?color_snake=00E5FF&color_dots=061024,0a1a33,0066FF,00E5FF,FFFFFF

      - name: Deploy to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
